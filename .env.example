# Copy to .env, then fill in at least ONE key. .env is gitignored and never committed.
# Providers are tried in the order listed in config/config.yaml.
GROQ_API_KEY=
OPENROUTER_API_KEY=
NVIDIA_API_KEY=
